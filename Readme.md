.then and async await both are used to handle the same thimg
async make the function retunrs the promise and await make the function wait for the promis response fulfilled or reject.

form is a very speial entity in HTML it has its default behavour. on submit id form uses its own submit action. in this case using onclick creates a conflict.


memory heap accessible
function accessible

